# samless
Use AWS SAM with improved DX. Install with:

```bash
npm i -g @mendes/samless
````

## Development

To install dependencies:

```bash
bun install
```

To build:

```bash
bun run build

```
To run:

```bash
bun run start

```
